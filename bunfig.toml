[install]
# 24h supply-chain guard: skip package versions published less than a day ago.
minimumReleaseAge = 86400
# Each entry bypasses the 24h guard for one package — keep this list empty unless a release must be unblocked intentionally.
minimumReleaseAgeExcludes = []
